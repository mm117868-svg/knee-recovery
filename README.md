# Knee Recovery (test build)

Static page: MediaPipe Pose for a whole-session knee angle, pixel-motion counter
for repetitions, written side by side. Everything runs in the visitor's browser;
no video leaves the device. Passcode gate on entry; noindex.

Serve with GitHub Pages (Settings > Pages > Deploy from branch, root).
